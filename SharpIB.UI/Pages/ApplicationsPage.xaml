<UserControl x:Class="SharpIB.UI.Pages.ApplicationsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <StackPanel>
        <TextBlock Text="Clients" Style="{StaticResource PageTitle}" />

        <!-- Search Bar -->
        <Border Background="#1E1E2E" CornerRadius="12" Padding="4" Margin="0,0,0,20">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Search clients..."
                     materialDesign:TextFieldAssist.HasClearButton="True"
                     materialDesign:HintAssist.IsFloating="False"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     FontSize="14" BorderThickness="0" />
        </Border>

        <!-- App List -->
        <Border Style="{StaticResource CardStyle}">
            <StackPanel>
                <!-- Header -->
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="80" />
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="100" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Client Name" FontSize="12" FontWeight="SemiBold" Foreground="#888" />
                    <TextBlock Grid.Column="1" Text="Billed Hours" FontSize="12" FontWeight="SemiBold" Foreground="#888" />
                    <TextBlock Grid.Column="2" Text="Invoices" FontSize="12" FontWeight="SemiBold" Foreground="#888" />
                    <TextBlock Grid.Column="3" Text="Status" FontSize="12" FontWeight="SemiBold" Foreground="#888" />
                    <TextBlock Grid.Column="4" Text="Action" FontSize="12" FontWeight="SemiBold" Foreground="#888" />
                </Grid>
                <Separator Background="#252540" Margin="0,0,0,8" />

                <ItemsControl ItemsSource="{Binding FilteredApps}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#16162E" CornerRadius="10" Padding="16,12" Margin="0,0,0,6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="80" />
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="100" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Client Name -->
                                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="AccountTie" Width="20" Height="20"
                                                                  Foreground="#7C4DFF" VerticalAlignment="Center" Margin="0,0,10,0" />
                                        <TextBlock Text="{Binding ProcessName}" FontSize="14" FontWeight="Medium"
                                                   Foreground="White" VerticalAlignment="Center" />
                                    </StackPanel>

                                    <!-- Duration -->
                                    <TextBlock Grid.Column="1" VerticalAlignment="Center"
                                               FontSize="14" FontWeight="SemiBold" Foreground="#00BFA5"
                                               Text="{Binding TotalDuration, StringFormat='{}{0:hh\\:mm\\:ss}'}" />

                                    <!-- Sessions -->
                                    <TextBlock Grid.Column="2" Text="{Binding SessionCount}" VerticalAlignment="Center"
                                               FontSize="14" Foreground="#888" />

                                    <!-- Category Badge -->
                                    <Border Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Left"
                                            CornerRadius="8" Padding="10,4">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#334455" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Level}" Value="Productive">
                                                        <Setter Property="Background" Value="#1B3A1B" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Level}" Value="Distracting">
                                                        <Setter Property="Background" Value="#3A1B1B" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock FontSize="11" FontWeight="Medium">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="{Binding CategoryName}" />
                                                    <Setter Property="Foreground" Value="#8899AA" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding CategoryName}" Value="{x:Null}">
                                                            <Setter Property="Text" Value="Uncategorized" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Level}" Value="Productive">
                                                            <Setter Property="Foreground" Value="#4CAF50" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Level}" Value="Distracting">
                                                            <Setter Property="Foreground" Value="#F44336" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>

                                    <!-- Assign Button -->
                                    <Button Grid.Column="4" VerticalAlignment="Center"
                                            Style="{StaticResource MaterialDesignFlatButton}"
                                            Command="{Binding DataContext.AssignCategoryCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding ProcessName}"
                                            Content="Assign" FontSize="11" Padding="8,4"
                                            Foreground="#7C4DFF" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Text="No clients listed yet. Start adding clients!"
                           FontSize="13" Foreground="#555" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding FilteredApps.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
